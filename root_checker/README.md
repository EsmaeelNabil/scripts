### This tool is a Bash script that checks if an Android device is rooted by verifying the presence of common root binaries and build tags. 

#### Usage:

```
./root_checker.sh
```

#### where
- You have a connected device/emulator
